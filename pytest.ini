[pytest]
pythonpath = .
# tests/integration/ needs the real docker-compose stack up (live
# Pinecone/Anthropic/Neo4j) - excluded from a plain `pytest` run, same as
# Phase 1's mocked suite staying separate from Phase 3's real-secrets CI
# job. Run it explicitly: pytest tests/integration
norecursedirs = tests/integration
